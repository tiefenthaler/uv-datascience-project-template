# Pre-commit configuration file.
# Run "uv run pre-commit install" to install pre-commit hooks. This sets up the hooks to run automatically before each commit.
# Run "uv run pre-commit run --all-files" to run all hooks manually.

default_stages: [pre-commit]

ci:
  autofix_commit_msg: 'ci: auto fixes from pre-commit.com hooks'
  autoupdate_commit_msg: 'ci: pre-commit autoupdate'

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.2
    hooks:
      - id: ruff
        name: Ruff check
        description: "Run 'ruff check' for extremely fast Python linting"
        args: [--fix, --exit-non-zero-on-fix] # Apply fixes and exit with non-zero if fix is not possible.

      - id: ruff-format
        name: Ruff format
        description: "Run 'ruff format' for extremely fast Python formatting"
        args: [--diff] # Show diff instead of applying changes directly.

  - repo: https://github.com/RobertCraigie/pyright-python
    rev: v1.1.394
    hooks:
      - id: pyright
        name: Pyright type checker
        language: system
        types: [python]
        fail_fast: true

  # - repo: local
  #   hooks:
  #     - id: pyright
  #       name: Type checker
  #       entry: uv run pyright
  #       language: system
  #       types: [python]
  #       fail_fast: true

  - repo: https://github.com/hadolint/hadolint
    rev: v2.12.0
    hooks:
      - id: hadolint
        name: Lint Dockerfiles
        description: Runs hadolint to lint Dockerfiles
        language: system
        types: ["dockerfile"]
        entry: hadolint
        # args:
        #   ["--config", ".hadolint.yaml"] # Use a configuration file for hadolint.
